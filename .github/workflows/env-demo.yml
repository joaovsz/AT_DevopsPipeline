name: Env Demo

on:
  push:

env:
  DEPLOY_ENV: staging

jobs:
  show-env:
    runs-on: ubuntu-latest
    steps:
      - name: Exibir ambiente
        run: |
          echo "Ambiente de deploy: $DEPLOY_ENV"
